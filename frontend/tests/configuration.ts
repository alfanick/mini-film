/** Keep server-free model tests separate from the browser matrix without duplicating file-selection rules. */
export const pureTestPatterns = [
  "**/geometry.spec.ts",
  "**/reconcile.spec.ts",
  "**/session.spec.ts",
  "**/*.unit.spec.ts",
];
